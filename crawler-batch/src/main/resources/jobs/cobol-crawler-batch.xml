<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
  xmlns:batch="http://www.springframework.org/schema/batch" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.springframework.org/schema/batch
    http://www.springframework.org/schema/batch/spring-batch.xsd
    http://www.springframework.org/schema/beans 
    http://www.springframework.org/schema/beans/spring-beans.xsd">

  <import resource="classpath:/spring-context.xml" />
  <import resource="classpath:/database.xml" />
  <!-- Spring should auto load this bean - this is required for JUnit test run-->
  <bean class="org.springframework.batch.test.JobLauncherTestUtils"/>
  <bean class="org.springframework.batch.core.scope.StepScope" />
  
  <batch:job id="cobolFileAnalyserJob">
    <batch:step id="listFiles">
      <batch:tasklet>
        <batch:chunk reader="fileListReader" writer="fileListWriter" processor="fileListProcessor" commit-interval="10" />
      </batch:tasklet>
    </batch:step>
    <!-- <batch:step id="maintainFileHistory">
      <batch:tasklet>
        <batch:chunk reader="cvsFileItemReader" writer="xmlItemWriter"
          processor="itemProcessor" commit-interval="10">
        </batch:chunk>
      </batch:tasklet>
    </batch:step>
    <batch:step id="crawlCobolFile">
      <batch:tasklet>
        <batch:chunk reader="cvsFileItemReader" writer="xmlItemWriter"
          processor="itemProcessor" commit-interval="10">
        </batch:chunk>
      </batch:tasklet>
    </batch:step> -->
  </batch:job>
  
  <bean id="fileListReader" class="com.vk.crawler.cobol.batch.FileListReader" scope="step"/>
  <bean id="fileListProcessor" class="com.vk.crawler.cobol.batch.FileListProcessor" scope="step"/>
  <bean id="fileListWriter" class="org.springframework.batch.item.database.JpaItemWriter">
    <property name="entityManagerFactory" ref="entityManagerFactory" />
  </bean>
  
</beans>